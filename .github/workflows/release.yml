name: Release package to pypi

on:
  push:
    tags:
    - "*.*.*"

jobs:
  release:
    runs-on: ubuntu-20.04

    steps:
    - uses: actions/checkout@v2
    - name: build & release
      run: ./scripts/release.sh
